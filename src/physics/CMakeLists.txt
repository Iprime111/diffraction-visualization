set(DIFFRACTION_VISUALIZE_PHYSICS_SOURCES propagation.cpp)

list(TRANSFORM DIFFRACTION_VISUALIZE_PHYSICS_SOURCES PREPEND ${CMAKE_CURRENT_SOURCE_DIR}/)

target_sources(diffraction-visualize PRIVATE ${DIFFRACTION_VISUALIZE_PHYSICS_SOURCES})
